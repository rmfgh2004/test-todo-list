spring:
  main:
    banner-mode: "off"
  datasource:
    url: jdbc:h2:mem:planning-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        generate_statistics: true
    open-in-view: false
    show-sql: false
